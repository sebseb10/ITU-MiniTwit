\section{Process' Perspective}
\byline{Andreas Schiøtt}

\subsection{CI/CD Pipeline}


\begin{table}[H]
      \centering
      \caption{CI/CD tools}
      \begin{tabularx}{\linewidth}{lX}
          \toprule
          \textbf{Technology} & \textbf{Purpose} \\
          \midrule
          GitHub Actions      & CI/CD automation \\
          Playwright          & end-to-end testing \\
          SonarCloud          & Static analysis and code coverage \\
          Hadolint            & Dockerfile linter \\
          ShellCheck          & Shell script linter \\
          Docker Build        & Build and push container images to Docker Hub \\
          Docker Swarm        & Automated docker container orchestration \\
          rsync               & Idempotent Sync files to the remote swarm manager \\
          Grafana             & Logging / monitoring running Prometheus, loki, Alloy \\
          Prometheus          & Metric collection and monitoring \\
          Loki                & Log aggregation system \\
          Grafana Alloy       & Log and monitoring collection \\
          \bottomrule
      \end{tabularx}
  \end{table}

Before cloud hosting, CI only built a docker container and ran unit tests against a local SQLite application.
\\\\
After cloud hosting on \textbf{DigitalOcean} was setup we added the CD pipeline to the CI pipeline. The CI/CD pipeline were both triggered concurrently on push to main branch
which then builds a \textbf{Docker image}, pushes it to \textbf{Docker Hub} and then deploys it to the DigitalOcean droplet using \textbf{SSH \& docker pull}. With the later focus on idempotent deployment,
we added a check to see if the CI pipeline succeeded before the CD pipeline is triggered.
\\\\
The CI pipeline was extended to include end-to-end testing using \textbf{Playwright}, and static code analysis using \textbf{SonarCloud} \& \textbf{Trivy}, \textbf{Hadolint} and \textbf{ShellCheck}. As shown in \cref{fig:ci}, on every push the pipeline detects which paths changed, builds the project, and then runs static analysis tests, SonarCloud analysis, and end-to-end tests in parallel.
\\\\
While writing the report we itterativly extended the CI pipeline to check which files were changed to decicde which steps to run, if only the report directory was changed we only run "build PDF" report" step and vice versa.
The pipeæline also publishes a self-contained GitHub release each week.
\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{CI.png}
    \caption{CI pipeline (\texttt{CI.yml}).}
    \label{fig:ci}
\end{figure}

\noindent The CD pipeline is triggered only after a successful CI run, ensuring that broken builds are never deployed. As shown in \cref{fig:cd}, it consists of three sequential jobs: building and pushing the Docker image (\textit{build-push}), deploying the application (\textit{Deploy}), and deploying the monitoring stack (\textit{Deploy-Monitoring}) with \textbf{Grafana}, \textbf{Prometheus}, \textbf{Loki} and \textbf{Grafana Alloy}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{CD.png}
    \caption{CD pipeline}
    \label{fig:cd}
\end{figure}

\noindent The \textit{Deploy} job applies the Docker Swarm stack file via \texttt{docker stack deploy}. 
Rolling-update semantics are covered in \cref{sec:architecture}. Detailed steps in \cref{fig:cd-deploy}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\linewidth]{CD_DeploySteps.png}
    \caption{Detailed steps of the \textit{Deploy} job.}
    \label{fig:cd-deploy}
\end{figure}


% Describe all stages and tools in your pipeline end-to-end.
% Include a diagram if helpful.
% Cover: triggering events, build, test, containerisation, push to registry,
%        deployment strategy (rolling, blue/green, etc.), release.

\subsection{Monitoring}
\href{http://165.245.209.232:3000/d/minitwit-dashboard/itu-minitwit-production?orgId=1&from=now-1h&to=now&timezone=browser&refresh=30s}{Link to Grafana monitoring dashboard.}
\noindent Building on the monitorign topology in \cref{sec:architecture}, Prometheus scrapes 
the application's \texttt{metrics} endpoint (job \texttt{itu-minitwit-app}) and Node exporter 
on each Swarm node. Grafana renders the dashboards.
\\

\noindent We have itterativly upated which data is collected and visualised in the Grafana dashboard based on the needs of the project.
At the projects end we have the Grafana dashboard organised into five sections, as shown in \cref{fig:monitoring}:
\begin{itemize}
    \item \textbf{Health overview} — application up/down status, average response time,
          requests / sec, in-flight requests, error rate and current memory usage.
    \item \textbf{HTTP traffic} — request rate by status code, response-time percentiles grouped by p50 p90 and p99.
     request rate /s grouped by HTTP method, and the response time grouped by api endpoints.
    \item \textbf{Error tracking}  error rates req/s grouped by 400/500 http response, errors mesured in req/s per endpoint.
    \item \textbf{Process \& runtime} — process CPU usage, process memory usage, thread count, garbage colllection rate.
    \item \textbf{Uptime \& Availability} — uptime \& availability over time (bar graf), Availability mesured in percentage over the selected time period.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{Monitoring.png}
    \caption{Grafana monitoring dashboard.}
    \label{fig:monitoring}
\end{figure}

\subsection{Logging}
\href{http://165.245.209.232:3000/d/itu-minitwit-logs/itu-minitwit-logs?orgId=1&from=now-1h&to=now&timezone=browser&var-container=$__all&var-level=.%2A&refresh=30s}{Link to Grafana logging dashboard.}
\noindent Grafana Alloy runs on every Swarm node, reads the docker socket, and ships container 
logs to Loki labelled with container, service, stream, and image. Loki rejects samples older than 
168 hours (7 days) to bound storage. 
\\\\    
\noindent The application logs the following events:
\begin{itemize}
    \item HTTP requests — method, path, response status code, and elapsed time mesured in ms.
    \item Unhandled exceptions — method, path, error details.
    \item Custom logs through \texttt{ILogger} throughout the minitwit codebase.
\end{itemize}

\noindent The logs are visualised in a Grafana dashboard with the following panels:
\begin{itemize}
    \item \textbf{Total Log lines \& error lines} — Total log lines \& errors within the selected time period
    \item \textbf{Log lines \& Errors/exceptions \& warnings per container} — Log lines \& errors grouped by container within the selected time period
    \item \textbf{log volume over time} — Log lines grouped by time (bar graph) within the selected time period
    \item \textbf{log \& Error viewer} —  Visulisation of all log lines and errors. 
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.4\textheight]{Logging.png}
    \caption{Loki log aggregation in Grafana.}
    \label{fig:logging}
\end{figure}



\subsection{Security Hardening}

We did not have the opportunity to implement hardened docker images as it is a paid feature in Docker Hub,
we instead tried to implement parts of the guide \href{https://cheatsheetseries.owasp.org/cheatsheets/Docker_Security_Cheat_Sheet.html}{OWASP Docker Security Cheat Sheet}
\noindent measures applied from Cheatsheet:
\begin{itemize}
    \item \textbf{Unprivilaged users in Docker containers} — Containers run as an unpriviledged user to prevent privilege escalation attacks.o
    \item \textbf{Prevent in-container privilege escalation} — use --security-opt=no-new-privileges on containers to prevent privilege escalation attacks.
    \item \textbf{Automatic Docker container update} - implementet a WeeklyHostUpdate which automatically updates the docker images on the swarm nodes to ensure that security updates are applied.
\end{itemize}

\noindent System uses applied to harden the system:
\begin{itemize}
    \item \textbf{GitHub Secrets} — database connection strings and API keys are stored as encrypted secrets and injected at deploy time.
    \item \textbf{Firewall} — inbound and outbound traffic on both application droplets and the database is restricted to only the ports and sources required.
    \item \textbf{TLS/HTTPS} — traffic to the application is encrypted using TLS Through the Nginx load balancer using Certbot.
    \item \textbf{Static code analysis} — SonarCloud and trivy are used to identify security vulnerabilities in the codebase/pipelines/docker images.
\end{itemize}

\noindent Static code analysis is integrated in the CI pipeline using SonarCloud \& Trivy.
Every build is analysed for bugs, coverage, code duplication and security vulnerabilities.
As shown in \cref{fig:sonarcloud}, there are still a number of security vulnerabilities but 
with only two of five members actively contributing, there was not time to address them before the deadline

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{SonarCube.png}
    \caption{SonarCloud quality gate results.}
    \label{fig:sonarcloud}
\end{figure}


% Brief description of measures taken, e.g.:
% - Secrets management (env vars, GitHub Secrets, Vault)
% - TLS / HTTPS setup
% - Firewall / network policies
% - Dependency scanning / vulnerability alerts
% - Least-privilege service accounts

\subsection{Availability and Scaling}

\byline{Andreas Schiøtt, Sebastian Holt Svendsen}
\noindent The application topology described in \cref{sec:architecture} already delivers our availability 
guarantees: keepalived VRRP between the two load balancers, two app replicas behind \texttt{least\_conn} 
routing, Swarm's \texttt{on-failure} restart policy for crashed containers, and rolling updates 
with a 10-seconds delay between replicas to keep at least one instance serving traffic.
% How do you handle availability (replication, health checks, restarts)?
% How do you scale (horizontal scaling, load balancing)?
% E.g. Docker Swarm replicas, Kubernetes HPA, DigitalOcean load balancer.
